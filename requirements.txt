anthropic>=0.40.0
rich>=13.0.0
python-dotenv>=1.0.0
colorama>=0.4.6

# ── Voix neurale (recommandé, gratuit) ──────────────────────────────────────
edge-tts>=6.1.9

# ── Voix premium (optionnel, nécessite ELEVENLABS_API_KEY dans .env) ────────
# elevenlabs>=1.0.0

# ── Reconnaissance vocale (optionnel) ───────────────────────────────────────
SpeechRecognition>=3.10.0
# pyaudio>=0.2.13       # nécessite portaudio — voir README

# ── Contrôle iPhone USB (optionnel) ─────────────────────────────────────────
# pymobiledevice3>=4.0.0

# ── Lecture audio Linux (choisir un parmi) ──────────────────────────────────
# mpg123 — via apt/brew (mpg123 -q fichier.mp3)
# ffmpeg  — via apt/brew (ffplay)
